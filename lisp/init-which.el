;;; init-which.el --- PATH SUPPORT -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package which-key
  :ensure t
  :init
  (which-key-mode))

(provide 'init-which)
;;; init-which.el ends here
